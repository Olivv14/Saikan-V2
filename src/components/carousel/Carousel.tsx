import { A11y, Keyboard, Pagination } from 'swiper/modules'; import { Swiper, SwiperSlide } from 'swiper/react'; import type { SwiperOptions } from 'swiper/types'
export function Carousel<T>({items,ariaLabel,renderItem,className='',options}:{items:T[];ariaLabel:string;renderItem:(item:T,index:number)=>React.ReactNode;className?:string;options?:SwiperOptions}){return <Swiper modules={[Keyboard,Pagination,A11y]} keyboard={{enabled:true}} pagination={{clickable:true}} a11y={{enabled:true}} spaceBetween={18} slidesPerView={1} className={className} aria-label={ariaLabel} {...options}>{items.map((item,index)=><SwiperSlide key={index}>{renderItem(item,index)}</SwiperSlide>)}</Swiper>}
