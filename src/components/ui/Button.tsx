import { Link } from 'react-router-dom'
import type { AnchorHTMLAttributes, ButtonHTMLAttributes, PropsWithChildren } from 'react'
type Common=PropsWithChildren<{variant?:'primary'|'secondary'|'ghost';size?:'md'|'lg';className?:string;loading?:boolean}>; type LinkProps=Common&AnchorHTMLAttributes<HTMLAnchorElement>&{href:string}; type BtnProps=Common&ButtonHTMLAttributes<HTMLButtonElement>&{href?:undefined};
const base='inline-flex min-h-11 items-center justify-center gap-2 rounded-full border px-5 text-sm font-semibold transition focus:outline-none focus:ring-2 focus:ring-white/70 disabled:cursor-not-allowed disabled:opacity-50'; const variants={primary:'border-white bg-white text-black hover:bg-white/90',secondary:'border-white/15 bg-white/5 text-white hover:bg-white/10',ghost:'border-transparent bg-transparent text-white hover:bg-white/10'};
export function Button(props:LinkProps|BtnProps){const {children,className='',variant='primary',size='md',loading=false,...rest}=props; const cls=`${base} ${variants[variant]} ${size==='lg'?'min-h-12 px-6 text-base':''} ${className}`; if('href' in props && props.href){const href=props.href; if(href.startsWith('/')) return <Link to={href} className={cls}>{loading?'Loading…':children}</Link>; return <a {...(rest as AnchorHTMLAttributes<HTMLAnchorElement>)} href={href} className={cls}>{loading?'Loading…':children}</a>} return <button {...(rest as ButtonHTMLAttributes<HTMLButtonElement>)} className={cls}>{loading?'Loading…':children}</button>}
